{
  "name": "erp-sales",
  "version": "1.0.0",
  "description": "Professional ERP for Sales and Customer Operations",
  "scripts": {
    "start": "node backend/server.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
